  ## V1.0验证逻辑核对文档审阅结论

  总体来看，这份文档能够准确描述当前三个测试用例的验证行为，也与已经通过的测试结果基本一致。

  但如果将其作为长期、通用的数据转换验证规范，仍有几项需要补充或澄清。

  ### 2026-07-28 落地状态

  本次审阅提出的 V1.0 定版项已经落实到验证代码和《V1.0验证逻辑核对.md》：

  - 固定 30 FPS 校验已补充 `frame_index` 文件顺序校验，并按唯一 Parquet Episode 读取全部行；
  - 图片和七维最近行查找已限制在所属 L1 唯一映射的 Parquet Episode 内，映射缺失或不唯一时直接失败；
  - 天机已增加四路 Parquet 图片 bytes/path 的全量数量和空值校验，并在每个 L1 首尾帧执行 MCAP、Parquet bytes、视频三方像素抽检；
  - MCAP 的 L1 闭区间裁剪已确认在最近邻和插值取值之前执行，无需修改算法；
  - 标注校验已根据期望 layers 动态要求并核对 `annotation.level_N_id/name`；
  - action 当前帧语义、topic 缺失边界、四元数顺序、SLERP、ZYX 和严格兜底标注规则已补充进核对文档；
  - 单元测试已补充 Episode 重叠/歧义、FPS 帧号、动态层级列和不可解码/空图片 bytes 场景。

  ### 1. 缺少完整的固定FPS时间轴校验

  目前文档主要验证：

  - 根据标注时间查找最近的Parquet行；
  - 使用该行的 original_timestamp_ns 回查MCAP；
  - 使用 timestamp 检查L1播放时长。

  但这些检查不能完整证明整个Episode采用了正确的固定FPS时间轴。

  建议增加以下校验。

  #### 时间轴定义

  T0 = L1范围内主相机第一条消息的log_time

  timeline_end =
      min(L1.endTimeNs, L1范围内主相机最后一条消息的log_time)

  第 n 帧应满足：

  timestamp[n] = n / fps

  original_timestamp_ns[n] =
      T0 + round(n / fps × 1_000_000_000)

  预期帧数为：

  floor((timeline_end - T0) × fps / 1_000_000_000) + 1

  还应检查：

  - frame_index 从0开始严格连续；
  - timestamp 不重复、不倒退、不跳帧；
  - original_timestamp_ns 严格递增；
  - timestamp 与 original_timestamp_ns 一一对应；
  - 实际帧数等于预期帧数。

  否则，少量图片和state/action抽检通过，并不能证明整个Episode都满足固定30 FPS要求。

  ### 2. 天机当前校验的是视频帧，不是Parquet内嵌图片

  文档中天机V2的图片来源实际是S3视频：

  MCAP原始图片 ↔ 输出视频解码帧

  它没有直接验证：

  MCAP原始图片 ↔ Parquet图片列中的bytes

  因此仍存在以下覆盖盲区：

  - 输出视频正确，但Parquet图片bytes缺失；
  - Parquet图片与行时间戳错位；
  - Parquet图片损坏或无法解码；
  - Parquet图片与视频帧不是同一图像。

  建议将文档表述修改为：

  > 天机V2当前图片校验对象是输出视频帧，尚未覆盖Parquet内嵌图片bytes的正确性。

  建议后续至少增加：

  - Parquet图片bytes存在性检查；
  - 图片bytes解码检查；
  - 图片数量与Parquet行数一致性检查；
  - 抽检MCAP原图与Parquet图片的像素一致性；
  - 抽检Parquet图片与同索引视频帧的一致性。

  ### 3. 查找最近Parquet行前，应限制所属Episode

  当前规则是在全部Parquet行中，寻找 original_timestamp_ns 距离标注时间最近的一行。

  对当前Episode时间范围不重叠的样例，这种方式可以工作。但在以下场景中可能选错：

  - 相邻Episode边界距离很近；
  - 两个Episode时间范围存在重叠；
  - 不同Episode来自重复MCAP数据；
  - 多个Episode存在相同或接近的时间戳；
  - 标注时间落在两个Episode输出范围之间。

  建议调整为：

  1. 根据L1标注确定目标 episode_id 或 episode_index；
  2. 只在该Episode对应的Parquet行中寻找最近时间；
  3. L2/L3先确定所属L1，再使用该L1对应的Episode；
  4. 如果一个L1无法唯一对应一个Episode，应直接报错，不能全局猜测。

  ### 4. Episode裁剪顺序需要明确

  文档虽然说明MCAP数据读取范围限制在所属L1内，但没有明确裁剪发生在插值之前。

  建议增加明确规则：

  按L1闭区间裁剪所有topic
  → 构建Episode内插值器
  → 生成固定FPS目标时间
  → 判断最近消息是否在同步阈值内
  → 使用最近值、插值或首末值夹取

  裁剪条件是：

  L1.startTimeNs <= message.log_time <= L1.endTimeNs

  任何L1范围外的消息都不能作为该Episode的插值前点或后点。

  这正是之前夹爪和摇操边界误差的主要来源。

  ### 5. topic缺失时不一定都导致转换失败

  文档目前写道：

  > 对应topic完全没有可用消息时，提取失败。

  这可以作为当前测试的严格要求，但不完全等同于转换端的通用处理规则。

  转换行为还可能受到以下配置影响：

  required
  effective_required
  missing_policy

  可能的策略包括：

  - 必需topic缺失：失败；
  - 非必需topic缺失：填零；
  - 使用前一帧值；
  - 根据配置执行其他降级处理。

  建议改为：

  > 当前三个测试构型要求参与七维校验的topic必须存在，因此缺失时测试失败。转换算法本身是否失败，还取决于该topic的必需性和缺失值策略。

  这样可以区分“测试验收要求”和“转换端容错行为”。

  ### 6. action语义需要补充

  文档应明确action是否具有下一帧语义。

  当前验证应按照以下规则：

  state目标时间  = 当前行original_timestamp_ns
  action目标时间 = 当前行original_timestamp_ns

  当前不应默认action使用下一帧。

  action可能有两种来源：

  - 存在独立action topic：按当前目标时间同步该topic；
  - 没有独立action数据且配置为 current_state：复制当前帧state。

  建议增加：

  > 当前V1.0验证中，action没有帧偏移，也不采用下一帧语义。若未来启用next_state，需要另行定义末帧处理、时间偏移和验证规则。

  ### 7. 姿态插值规则需要写得更精确

  文档已经写明使用四元数SLERP，但建议进一步固定以下契约：

  原始四元数顺序：qx, qy, qz, qw
  旋转插值：SLERP
  欧拉角转换顺序：ZYX
  欧拉角中间结果：yaw, pitch, roll
  Parquet输出顺序：roll, pitch, yaw
  角度单位：弧度

  还应明确使用大写 "ZYX"，不能替换成小写 "zyx"：

  rotation.as_euler("ZYX")

  大小写代表不同的旋转定义，不能混用。

  ### 8. 动态标注层级列需要完整校验

  文档目前只明确要求：

  annotation.level_1_id
  annotation.level_1_name

  但多层级标注可能还包含：

  annotation.level_2_id
  annotation.level_2_name
  annotation.level_3_id
  annotation.level_3_name
  ...

  建议规则调整为：

  > full_annotation声明了多少个层级，Parquet就必须包含对应数量的 annotation.level_N_id 和 annotation.level_N_name 列。

  例如三层标注必须同时检查：

  annotation.level_1_id
  annotation.level_1_name
  annotation.level_2_id
  annotation.level_2_name
  annotation.level_3_id
  annotation.level_3_name

  不能只依赖 annotation.hierarchy_json 和leaf字段间接确认层级信息。

  ### 9. 标注验证可以有意比转换兜底逻辑更严格

  转换过程中可能存在异常兜底标注，例如：

  - 只写入Episode级信息；
  - 标注ID为空；
  - 起止时间退化为当前帧时间；
  - 没有完整层级信息。

  当前文档要求：

  - 标注ID不能为空；
  - 开始时间必须早于结束时间；
  - hierarchy必须有效；
  - 各平铺字段必须与hierarchy一致。

  这些严格要求是合理的。建议增加说明：

  > 标注验证规则有意比转换端异常兜底逻辑更严格。若产物命中空ID、零时长或不完整层级的兜底标注，应判定转换结果不合格。

  ### 10. 天机的L2/L3归属方式需要区分测试实现和通用规则

  当前天机测试使用 current_sequence 将L2/L3关联到所属L1。这可以作为当前测试数据的实现方式，但不建议定义为唯一通用规则。

  建议通用归属优先级为：

  1. 明确的 episode_id；
  2. 明确的父标注关系；
  3. current_sequence；
  4. 时间范围包含关系。

  如果归属结果不唯一，应直接失败，不应任意选择某个L1。

  ## 已确认一致的内容

  以下规则与当前转换结果一致，可以保留：

  - MCAP消息时间统一使用 log_time；
  - topic先按所属L1闭区间裁剪；
  - 图片按目标时间取最近消息；
  - state/action使用Parquet行的 original_timestamp_ns；
  - 不二次吸附到原始主相机帧；
  - sync_policy="3" 表示3ms；
  - exact_frame 当前也按3ms处理；
  - 最近消息距离小于等于阈值时使用最近值；
  - xyz使用线性插值；
  - 夹爪角度使用线性插值；
  - 姿态使用四元数SLERP；
  - Episode边界使用Episode内首值或末值；
  - state/action通过 st_display/ac_display 解释字段；
  - 输出七维顺序为 x,y,z,roll,pitch,yaw,angle；
  - frame_index/index 在每个Episode文件内从0开始；
  - done=True只出现在Episode最后一行；
  - 标注激活区间采用闭区间；
  - subtask_progress限制在 [0,1]；
  - 当前业务Parquet采用单Episode单文件输出；
  - 七维绝对误差阈值为 0.0005；
  - L1播放时长误差阈值为 0.07s。

  ## 建议的定版条件

  建议至少完成以下四项后，再将文档作为正式V1.0验证规范：

  1. 增加完整固定FPS时间轴校验；
  2. 最近Parquet行查找限制在所属Episode内；
  3. 明确天机当前只验证视频，并增加Parquet图片校验；
  4. 明确L1裁剪先于最近邻和插值。

  之后再补充：

  - 动态标注层级列；
  - topic缺失策略；
  - action时间语义；
  - SLERP和ZYX的准确约定；
  - 异常兜底标注的严格失败规则。
