# V1.0 验证逻辑核对

## 1. 文档范围

本文档只包含图片校验、七维数据校验和标注数据校验，不包含任务查询、标注创建、质检、转换发起和转换状态轮询逻辑。

三类校验均读取 S3 `final-data` 下 `data/chunk-000` 中的全部 Parquet 文件。原始对照数据来自任务 `original-data` 下的全部 indexed MCAP 文件。

## 2. 图片校验逻辑

### 2.1 统一的时间对齐逻辑

当前代码不直接拿标注时间分别在 Parquet 和 MCAP 中独立找帧，而是按以下顺序统一时间基准。

1. 在全部 Parquet 文件和全部 row group 的 `original_timestamp_ns` 列中查找距离标注时间绝对差最小的一行。
2. 记录该行的 `original_timestamp_ns`，作为 `matched_timestamp_ns`。
3. 从该 Parquet 行提取对应相机图片。
4. 使用同一个 `matched_timestamp_ns` 作为 MCAP 图片目标时间。
5. 在每个相机 topic 中按 MCAP 消息 `log_time` 查找距离目标时间绝对差最小的图片消息。
6. 将 MCAP 图片解码为 PNG，并与 Parquet 图片进行像素比较。

最近行匹配只比较绝对时间差。两个候选行时间差相同时，保留扫描过程中先遇到的行。

MCAP 图片读取使用 chunk index 和时间范围读取，不允许无索引地顺序读取完整远端对象。默认最大搜索窗口为目标时间前后 1 秒，搜索窗口会从较小范围逐级扩大。标注所属 L1 Episode 的起止时间同时作为搜索边界。窗口内缺少某个相机 topic 时，代码会在该 L1 Episode 范围内继续查找该 topic。

夹爪 V2 使用 L1 起止时间作为 MCAP 搜索边界。Shake V3 使用 L1 起止时间作为搜索边界。天机 V2 的非 L1 标注按 `current_sequence` 唯一关联到所属 L1，并使用所属 L1 的起止时间作为搜索边界。

### 2.2 Parquet 图片来源

夹爪 V2 和 Shake V3 批量读取待校验时间点，自动发现 Parquet 图片列，并按相机标识保存对应图片。

天机 V2 明确使用机器人构型中的相机 `name` 作为 Parquet 图片列名，并优先从 S3 视频文件读取和解码目标帧，输出 PNG 后参与比较。

批量提取会复用已经读取的 Parquet row group，减少同一 row group 的重复远端读取。每个提取结果必须存在 `matched_timestamp_ns`，并且必须生成实际存在的图片文件，否则当前时间点校验失败。

### 2.3 MCAP 相机来源

参与校验的 MCAP 相机 topic 从对应机器人构型的 `cameras` 配置读取。夹爪 V2 和 Shake V3 按 `left`、`right` 分组解析相机。天机 V2 读取构型中的全部相机，并校验相机 `key`、`name` 和 `topic` 均不为空且不重复。

### 2.4 夹爪 V2 和 Shake V3 的图片通过条件

夹爪 V2 和 Shake V3 调用公共图片比较函数的默认规则，处理过程如下。

1. MCAP 图片作为参考图，Parquet 图片作为候选图。
2. 两张图片先转换为 RGB。
3. 原始宽高必须完全一致。宽高不一致时不执行像素比较，直接判定失败。
4. 对每个像素分别计算 R、G、B 三个通道的绝对差。
5. 一个像素三个通道中的最大绝对差不超过 10 时，该像素计为匹配像素。
6. 匹配像素数除以总像素数得到像素匹配率。
7. 所有像素和所有 RGB 通道的绝对差平均值作为平均绝对误差。
8. 像素匹配率必须大于等于 95%。
9. 平均绝对误差必须小于等于 3.0。
10. 像素匹配率和平均绝对误差两个条件必须同时满足，图片才判定为一致。

最大像素误差只作为结果指标记录，不单独参与通过判定。图片失败时生成将绝对差放大 4 倍的 PNG 差异图。

### 2.5 天机 V2 的图片通过条件

天机 V2 针对 MCAP 原图与转换后视频帧的尺寸差异，启用了独立的预处理和阈值。

1. MCAP 图片作为参考图，Parquet 图片作为候选图。
2. 两张图片先转换为 RGB。
3. MCAP 参考图尺寸不同时，使用 BICUBIC 重采样缩放到 Parquet 图片尺寸。
4. 缩放后的 MCAP 图片和 Parquet 图片均执行半径为 1.0 的高斯平滑。
5. 每个像素三个 RGB 通道中的最大绝对差不超过 10 时，该像素计为匹配像素。
6. 像素匹配率必须大于等于 90%。
7. 所有像素和所有 RGB 通道的平均绝对误差必须小于等于 4.0。
8. 像素匹配率和平均绝对误差两个条件必须同时满足，图片才判定为一致。

天机图片校验通过后会删除本次成功比较使用的 MCAP 和 Parquet 临时图片。校验失败时保留 MCAP 图片、Parquet 图片和差异图，并在结果中记录尺寸、缩放方式、高斯平滑参数、匹配率、平均绝对误差和最大绝对误差。

### 2.6 图片整体判定

任意图片提取失败、文件不存在、解码失败、尺寸规则不满足或像素阈值不满足，都会记录为失败。脚本会继续收集同一校验步骤中的其他图片结果，最后汇总失败项并使该测试步骤失败。

## 3. 七维数据校验逻辑

### 3.1 校验对象

代码比较 `action` 和 `observation_state` 中左右机械臂的数据，统一使用以下七维顺序。

| 数据区段 | 机械臂 | 七维顺序 |
| --- | --- | --- |
| `action` | left | x、y、z、roll、pitch、yaw、angle |
| `action` | right | x、y、z、roll、pitch、yaw、angle |
| `observation_state` | left | x、y、z、roll、pitch、yaw、angle |
| `observation_state` | right | x、y、z、roll、pitch、yaw、angle |

### 3.2 统一的时间对齐逻辑

七维数据采用与图片校验一致的 Parquet 主导时间对齐方式。

1. 使用标注 `startTimeNs` 或 `endTimeNs` 在全部 Parquet 的 `original_timestamp_ns` 中查找全局最近行。
2. 取得该行的 `matched_timestamp_ns`。
3. 从该行读取 Parquet 向量。
4. 使用同一个 `matched_timestamp_ns` 作为 MCAP 机器人数据的采样参考时间。
5. MCAP 提取时设置 `align_to_main_camera=False`，不再把该时间二次吸附到不等间隔的原始主相机帧。

此逻辑以转换输出的固定时间轴采样点为准，使 MCAP state 和 action 与实际 Parquet 行处于同一采样时刻。

MCAP 数据读取范围限制在标注所属 L1 Episode 内。夹爪 V2 的全部标注使用唯一 L1 范围，Shake V3 使用唯一 L1 范围，天机 V2 的 L2 和 L3 根据 `current_sequence` 映射到唯一所属 L1 范围。

### 3.3 MCAP 七维值生成逻辑

机器人构型同时定义 `action` 和 `observation_state` 的左右臂位姿 topic 与夹爪 topic。代码按构型读取对应字段并生成七维向量。

位姿原始数据为三维位置加四元数。代码保留 x、y、z，并将四元数按 ZYX 欧拉角顺序转换为 roll、pitch、yaw。夹爪字段转换为 angle。最终输出顺序固定为 x、y、z、roll、pitch、yaw、angle。

构型中的 `sync_policy="3"` 按 3 毫秒转换为 3,000,000 纳秒容差。`exact_frame` 在当前公共模块中同样转换为 3 毫秒容差。

每个 topic 的取值规则如下。

1. 距离采样参考时间最近的消息在同步容差内时，直接使用最近消息。
2. 最近消息超出同步容差，且采样时间前后均有消息时，使用前后消息插值。
3. 位移使用线性插值，四元数使用球面线性插值，夹爪角度使用线性插值。
4. Episode 边界处只有一侧消息时，使用边界侧的首个值或最后一个值。
5. 对应 topic 完全没有可用消息时，提取失败。

远端 MCAP 读取要求 chunk index，不允许无索引完整顺序读取。代码先在逐级扩大的时间窗口中查找，常规窗口最大为采样时间前后 1 秒。位姿或夹爪 topic 缺少满足取值条件的前后消息时，代码会将缺失 topic 的读取范围扩展到所属 L1 Episode，仍缺少可用消息时提取失败。

### 3.4 Parquet 七维值生成逻辑

Parquet 最近行必须包含以下列。

- `timestamp`
- `original_timestamp_ns`
- `actions`
- `state`
- `ac_display`
- `st_display`

`actions` 对应 `action`，`state` 对应 `observation_state`。代码使用 `ac_display` 和 `st_display` 的字段名识别左右臂及七维标签，再从 `actions` 和 `state` 的对应下标取值。

字段名必须能识别出 left 或 right，并且必须能识别出 x、y、z、roll、pitch、yaw、angle。两侧每组均必须完整包含七个不同标签。展示字段数量必须与源向量长度一致。任一字段无法识别、标签重复、标签缺失或长度不一致时，提取失败。

### 3.5 数值比较和通过条件

MCAP 和 Parquet 按 `section`、`group` 和七维标签对齐。固定比较顺序为 action 左臂、action 右臂、observation_state 左臂、observation_state 右臂。

每一维计算公式为绝对差等于 MCAP 值减 Parquet 值的绝对值。当前固定绝对误差容差为 `5e-04`，即 0.0005。

每一维绝对差小于等于 0.0005 时通过。当前逻辑不使用相对误差，也不根据量纲设置不同阈值。全部参与比较的维度必须通过。任一维度存在超差，或出现字段缺失、topic 缺失、向量结构错误、向量数量错误和标签错误时，对应测试步骤失败。

Shake V3 还额外断言 MCAP 提取结果中的位姿 topic、夹爪 topic 和位姿字段与 `遥操机械臂.json` 的当前固定配置一致。

## 4. 标注数据校验逻辑

### 4.1 期望标注来源

正常执行标注流程时，期望标注直接取脚本实际提交的 `annotation_json.layers`，其中包含运行时生成的标注 ID、描述和时间。

任务已经处于已转换状态时，标注创建和提交步骤被跳过。此时脚本根据代码中的固定标注配置重新构造期望 layers。重构数据不包含运行时标注 ID，因此已转换任务的兜底校验不比较标注 ID，仍比较层级、层标签、描述、开始时间和结束时间。

三个脚本调用标注比较时均启用 L1 播放时长校验。

### 4.2 Parquet 必需标注列

每个 Parquet 文件均独立读取和校验以下列。

- `original_timestamp_ns`
- `episode_index`
- `frame_index`
- `index`
- `episode_id`
- `task`
- `subtask`
- `subtask_id`
- `subtask_progress`
- `annotation.level_1_id`
- `annotation.level_1_name`
- `annotation.leaf_id`
- `annotation.leaf_name`
- `annotation.path`
- `annotation.hierarchy_json`
- `done`

缺少任一必需列都会记录校验失败。`timestamp` 列存在时同时读取，用于 L1 播放时长校验。三个脚本均启用播放时长校验，因此缺少有效 `timestamp` 值会导致 L1 播放时长校验失败。

### 4.3 每个 Parquet 文件的逐行校验

每个 Parquet 文件的行号从 0 独立开始，当前代码执行以下检查。

1. `original_timestamp_ns` 必须是有效整数，并且在文件内严格递增。
2. `frame_index` 必须等于当前文件内行号。
3. `index` 必须等于当前文件内行号。
4. 每行使用非空 `episode_id` 作为 Episode 标识，`episode_id` 为空时使用 `episode_index`，两者均为空时失败。
5. `done` 必须是布尔值。
6. 每个 Episode 只能在最后一行出现 `done=True`，其他行必须为 `done=False`。
7. `subtask_progress` 必须存在、能够转换为数值并处于 0 到 1 的闭区间。
8. 同一个 `subtask_id` 的 `subtask_progress` 在文件扫描顺序中不能倒退。
9. `annotation.hierarchy_json` 必须能够解析为非空数组。
10. hierarchy 中每一项必须是对象。
11. 每个层级的 `level` 必须是正整数。
12. `layer_id` 必须等于 level 对应的 `l1`、`l2`、`l3` 等形式。
13. 每个层级的标注 ID 和名称均不能为空。
14. 每个层级的开始时间和结束时间必须是有效整数，并且开始时间早于结束时间。
15. 当前行 `original_timestamp_ns` 必须落在该行 hierarchy 中每个已激活标注的闭区间内。
16. 同一行 hierarchy 中已出现层级必须严格递增且不能重复。
17. L3 当前帧不要求完整落在某个 L2 区间内，因此 hierarchy 允许省略当前帧未激活的中间层。
18. `task`、一级标注 ID、一级标注名称、`subtask`、叶子标注 ID 和叶子标注名称必须与 hierarchy 的首层和末层一致。
19. `annotation.path` 必须按 hierarchy 顺序包含各层级名称。
20. 同一个 level、layer_id 和标注 ID 不能对应多个名称或多个时间定义。

逐行校验发现的任何错误都会进入最终失败列表。单个文件最多展示前 100 条具体错误，超过部分记录未展示数量。

### 4.4 多个 Parquet 文件的汇总逻辑

每个文件先按本文件局部行号完成校验，再汇总全部文件。相同标注按照以下六个字段组成的完整键合并。

- level
- layer_id
- id
- name
- start_timestamp_ns
- end_timestamp_ns

合并时累计行数和来源文件，并取最早、最晚的原始时间及播放时间。最终得到跨全部 Parquet 文件的唯一标注列表和各层标注数量。

### 4.5 期望标注与实际标注比较

期望标注会归一化为 level、layer_id、id、name、start_timestamp_ns、end_timestamp_ns 六个字段，并按层级、开始时间和 ID 排序。

实际标注从 Parquet hierarchy 汇总结果中取得。比较逻辑如下。

1. 先比较每个层级的期望数量和实际数量，固定覆盖 L1、L2、L3，并包含数据中出现的其他层级。
2. 每条期望标注先在同层级实际标注中寻找候选项。
3. 期望 ID 非空时优先使用相同 ID 的候选项。
4. 名称、开始时间和结束时间完全相同时优先使用该候选项。
5. 最终逐项比较层级、层标签、标注 ID、描述、开始时间和结束时间。
6. 期望 ID 为空时跳过 ID 字段比较。
7. 每条实际标注只能匹配一条期望标注。
8. 未找到的期望标注记为失败。
9. 未被使用的实际标注视为代码未提交的额外标注并记为失败。

除 ID 兜底规则外，描述和时间均执行精确相等比较，没有文本模糊匹配和时间容差。

### 4.6 L1 播放时长校验

每个 L1 标注的代码期望时长等于 `endTimeNs - startTimeNs`。

Parquet 实际播放时长等于该 L1 标注所有命中行中最大的 `timestamp` 减去最小的 `timestamp`。代码计算期望时长与实际播放时长的绝对误差，允许误差为 0.07 秒。绝对误差小于等于 0.07 秒时通过，超过 0.07 秒时失败。

## 5. 最终失败条件汇总

以下任一情况出现时，对应校验步骤失败。

- 任一目标时间找不到可用 Parquet 行。
- 任一必需 Parquet 字段或图片列缺失。
- 任一 MCAP 必需 topic 在有效 Episode 范围内没有可用消息。
- 任一图片无法解码或未生成实际文件。
- 任一图片不满足对应脚本的尺寸、像素匹配率和平均绝对误差条件。
- 任一七维向量结构、标签、字段映射或数量不符合要求。
- 任一七维数值绝对差超过 0.0005。
- 任一 Parquet 标注逐行结构校验失败。
- 任一层级标注数量、描述、时间或需要比较的 ID 与期望不一致。
- Parquet 中出现代码期望之外的额外标注。
- 任一 L1 的 Parquet 播放时长与代码标注时长误差超过 0.07 秒。
