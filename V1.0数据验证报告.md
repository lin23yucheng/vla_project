# 夹爪、摇操、天机数据转换验证报告

**报告版本：** V1.0  
**报告日期：** 2026年7月28日  
**验证结论：** 夹爪、摇操、天机三类数据均已完成验证，全部检查项通过，可以进入后续使用环节。

## 一、验证目的

本次验证的目标，是确认机器人原始采集数据经过格式转换后，画面、机械臂运动数据和人工标注均被正确保留，没有出现错帧、错位、数值失真、标注丢失或时间关系改变。

验证不是只检查“文件是否生成”，而是把转换前后的实际内容放在同一时间点逐项对照，确认结果真实可用。

## 二、验证范围

| 数据类型 | 相机数量 | 覆盖标注 | 检查时间点 | 图像检查 | 运动数值检查 | 结果 |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
| 夹爪 | 2 路 | 3 段 | 6 个 | 12 / 12 通过 | 168 / 168 通过 | 通过 |
| 摇操 | 2 路 | 1 段 | 2 个 | 4 / 4 通过 | 56 / 56 通过 | 通过 |
| 天机 | 4 路 | 9 段 | 18 个 | 72 / 72 通过 | 504 / 504 通过 | 通过 |
| **合计** |  | **13 段** | **26 个** | **88 / 88 通过** | **728 / 728 通过** | **全部通过** |

每段标注均检查开始和结束两个边界。边界位置最容易出现取到上一段或下一段数据、首尾数据缺失等问题，因此本次检查重点覆盖这些高风险位置，而不是随机取样。

表中的图像检查数量按“相机 × 标注边界时间点”统计。天机除上述 72 组原始 MCAP 画面与输出视频画面对比外，还单独执行了 Parquet 图片字段全量完整性检查，以及 2 个完整动作首尾、4 路相机共 16 组三方图像抽检；这些新增检查在后文单列，不与 72 组基础图像检查重复相加。

## 三、验证了哪些方面

### 1. 转换结果完整性

确认三类任务均已完成转换，结果文件能够正常读取，所需的图片、运动数据、时间信息和标注信息均存在。缺少必要内容、文件损坏或内容无法读取时，都会直接判定失败。

天机还对全部 Parquet 图片字段进行了完整性检查：2 个 Episode 共 2257 行，4 路相机的图片 `bytes` 和 `path` 应各有 9028 项，实际均为 9028 / 9028 项且空值为 0。完整动作首尾抽取的图片 `bytes` 也均可正常解码。

### 2. 时间顺序和输出频率

确认每一段完整动作都从该段主相机的第一张有效画面开始，按照每秒 30 条的固定节奏连续输出，直到该段结束或主相机画面结束。每段完整动作必须唯一对应一个 Parquet Episode；如果没有对应结果或同时对应多个结果，验证直接失败，不跨 Episode 猜测最近数据。

重点检查：

- 第一条数据是否从正确时间开始；
- 是否按文件原始顺序读取，`frame_index` 是否从 0 开始逐行连续；
- 播放时间是否严格等于帧号除以 30，原始时间是否落在从主相机首帧开始的 30 FPS 时间网格上；
- 是否存在重复、跳过、顺序倒退或时间戳错位；
- 输出总帧数是否与动作结束时间或主相机末帧时间相符；
- 图片、运动状态和控制信息是否使用同一个目标时间。

三类数据的时间顺序和固定输出频率检查均已通过。

### 3. 相机图像

对每段标注的开始和结束时刻，先在转换结果中找到时间最接近的一条记录，再以这条记录的实际时间回到原始数据中寻找对应相机画面。

每路相机独立寻找离目标时间最近的画面，但只能在当前完整动作范围内取图，不能误取上一段或下一段动作的画面。这样可以保证比较的是同一动作、同一时刻附近的真实画面。

取到图片后进行逐像素比较：

| 检查项 | 夹爪、摇操 | 天机 |
| --- | --- | --- |
| 图片尺寸 | 转换前后必须一致 | 原始图调整到转换后尺寸 |
| 允许轻微画面处理 | 不处理 | 允许轻度平滑，以消除缩放和视频压缩影响 |
| 单个像素颜色差 | 不超过 10 个色阶 | 不超过 10 个色阶 |
| 达标像素比例 | 不低于 95% | 不低于 90% |
| 全图平均颜色误差 | 不高于 3.0 | 不高于 4.0 |

天机的原始画面和转换后视频画面尺寸不同，且视频编码会产生轻微色差，因此比较前进行统一尺寸和轻度平滑。这只是图片保存方式不同，不代表天机使用了不同的取图时间规则。

验证结果：夹爪和摇操的转换前后图片完全一致；天机 72 组 MCAP 原始画面与输出视频画面对比全部达到规定标准，其中最低达标像素比例为 90.531%，最高平均颜色误差为 3.6488，均在允许范围内。

为同时确认天机 Parquet 内嵌图片不是只“有字段”而是内容正确，本次还在 2 个完整动作的开始和结束时刻，对 4 路相机执行 MCAP 原始画面、Parquet 图片 `bytes` 和输出视频帧三方对照，共 16 组三方样本。新增的两类对比结果如下：

| 新增对比 | 通过数量 | 最低达标像素比例 | 最高平均颜色误差 |
| --- | ---: | ---: | ---: |
| MCAP 原始画面 ↔ Parquet 图片 `bytes` | 16 / 16 | 98.806% | 1.1550 |
| Parquet 图片 `bytes` ↔ 输出视频帧 | 16 / 16 | 92.882% | 3.3752 |

两类新增对比均满足天机图片标准。它们用于证明 Parquet 内嵌图片和输出视频分别与原始画面保持一致，不作为额外标注时间点计入前述 72 组基础图像检查。

### 4. 机械臂和夹爪运动数据

每个检查时间点同时核对四组数据：左侧控制值、右侧控制值、左侧实际状态和右侧实际状态。

每组包含七项：

1. 三个位置值，用于描述机械臂末端在空间中的位置；
2. 三个姿态值，用于描述机械臂末端的朝向；
3. 一个夹爪开合值。

三类数据使用完全相同的取值规则：

1. 图片、实际状态和控制值均以转换结果中同一条记录的时间为准，不各自选择不同目标时间。
2. 左右两侧分别从各自的数据来源取值，不混用左右数据。
3. 只允许使用当前完整动作时间范围内的数据。较短的二级、三级标注也使用其所属完整动作的范围，不能跨到其他动作。
4. 如果目标时间附近 3 毫秒内有数据，直接取距离最近的一条。
5. 如果最近数据超过 3 毫秒，但目标时间前后都有数据，则根据目标时间在前后两条数据之间的位置进行计算。
6. 位置和夹爪开合值按前后变化比例计算；姿态按真实旋转轨迹计算，避免直接平均三个角度造成方向跳变。
7. 如果目标时间早于当前动作内第一条有效数据，则取第一条；如果晚于最后一条，则取最后一条。不会借用动作范围外的数据补齐。
8. 控制值与实际状态使用同一个当前时间，不额外取下一帧。

转换结果中的七项数据不是按固定数组位置直接猜测，而是先确认每个数值代表左侧还是右侧、位置还是姿态、控制还是状态，再逐项对应，避免因排列变化造成误判。

每一项转换前后的绝对差必须不超过 0.0005，等于该值仍视为通过。任何一侧、任何一组、任何一项超出标准，所在时间点都会判定失败。

验证结果：三类数据共 728 项运动数值全部通过，未通过项为 0。

### 5. 人工标注

将转换结果中的标注与任务原始标注逐段核对，检查：

- 标注层级是否完整；
- 任务声明的每一级标注是否都有对应的 `annotation.level_N_id` 和 `annotation.level_N_name` 字段；
- 每行已激活层级的平铺字段是否与完整层级信息一致，未激活层级是否保持为空；
- 每层标注数量是否一致；
- 每段标注是否属于正确的上级动作；
- 动作描述是否一致；
- 开始和结束时间是否一致；
- 是否存在缺失或多余标注；
- 完整动作在转换结果中的播放时长是否与标注时长一致。

播放时长允许误差不超过 0.07 秒。夹爪最大误差为 0.0137 秒，摇操为 0.0260 秒，天机两段完整动作分别为 0.0453 秒和 0.0280 秒，均满足要求。

三类数据的标注层级、动态层级字段、数量、动作描述、归属和时间边界全部通过。

## 四、三类数据规则是否一致

夹爪、摇操和天机的核心验证规则一致，包括：

- 使用相同的时间基准；
- 按每秒 30 条检查输出时间顺序；
- 图片和运动数据使用同一目标时间；
- 在当前完整动作范围内取最近值、计算中间值或使用首尾值；
- 运动数据均检查左右两侧、控制值和实际状态；
- 每组均检查位置、姿态和夹爪开合七项数据；
- 使用相同的数值误差标准；
- 使用相同的标注完整性标准。

三类数据之间的差别仅在于相机数量、原始设备来源、图片保存方式和标注层级数量。天机图片需要适配视频尺寸和编码差异，但取图时间及运动数据取值规则没有区别。

## 五、验证结果汇总

| 验证方面 | 夹爪 | 摇操 | 天机 | 总体结果 |
| --- | --- | --- | --- | --- |
| 转换结果完整性 | 通过 | 通过 | 通过 | 通过 |
| 时间顺序和固定输出频率 | 通过 | 通过 | 通过 | 通过 |
| 图像内容一致性 | 12 / 12 | 4 / 4 | 72 / 72 | 88 / 88 通过 |
| Parquet 图片字段完整性 | 不适用 | 不适用 | 9028 / 9028 `bytes`、9028 / 9028 `path`，空值 0 | 通过 |
| 完整动作首尾三方图像抽检 | 不适用 | 不适用 | 16 / 16 组三方样本 | 通过 |
| 运动数据一致性 | 168 / 168 | 56 / 56 | 504 / 504 | 728 / 728 通过 |
| 标注完整性 | 通过 | 通过 | 通过 | 通过 |

## 六、最终结论

本次验证确认：

1. 三类数据均已正确完成转换，结果文件完整且能够正常读取；
2. 转换前后的相机画面对应同一动作时刻；天机 Parquet 内嵌图片和输出视频均与原始画面一致，未发现错帧、串段、空图片或画面异常丢失；
3. 左右机械臂的位置、姿态、夹爪开合、控制值和实际状态均在规定误差内保持一致；
4. 多层级标注的内容、归属、动态层级字段和时间范围均被完整保留；
5. 夹爪、摇操和天机使用统一的核心取值规则，不存在三套不同的运动数据处理标准；
6. 全部 88 组基础图像、天机 16 组三方图像样本、天机 Parquet 图片 `bytes` 和 `path` 各 9028 项完整性检查、728 项运动数值及标注检查均通过，未发现影响后续使用的问题。

**综合判定：三类数据转换结果符合验收要求，可用于后续模型训练、数据分析和业务交付。**
