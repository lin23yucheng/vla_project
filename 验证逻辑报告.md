# 夹爪、摇操、天机数据转换验证逻辑报告

**版本：** V2.0

**更新日期：** 2026年7月28日

**用途：** 供测试与开发逐项核对当前转换实现和验证实现。

**当前状态：** 夹爪 V2、摇操 V3、天机 V2 已按本文规则执行完成，全部通过。

## 1. 结论和统一口径

夹爪、摇操、天机的核心数据规则相同，不存在三套不同的图片或七维采样算法。三者只在构型配置、相机数量、topic、夹爪字段、输出版本和图片载体上存在差异。

当前统一口径如下：

1. MCAP 消息筛选、最近邻、前后点和插值比例全部使用 `message.log_time`。
2. `message.publish_time` 仅用于诊断展示，不参与预期值计算。
3. 每个 topic 必须先裁剪到所属父 L1 Episode 的闭区间 `[startTimeNs, endTimeNs]`。
4. L2/L3 抽检使用其所属父 L1 的范围，不使用 L2/L3 自身范围。
5. 标注边界先命中最近的 Parquet 行，再使用该行 `original_timestamp_ns` 作为图片、state 和 action 的共同目标时间。
6. state/action 不再二次吸附到主相机实际帧。
7. 机器人 state/action topic 的 `sync_policy="3"` 和 `sync_policy="exact_frame"` 在当前转换逻辑中都表示 `3 ms`。
8. 最近消息时间差 `<=` topic 阈值时取最近值；只有严格大于阈值时才进入插值或边界钳制。
9. 位置和夹爪值线性插值；姿态对四元数执行最短路径 SLERP，然后按内禀大写 `ZYX` 转欧拉角。
10. action 使用当前 Parquet 行目标时间，不做下一帧偏移。
11. 七维顺序统一为 `x, y, z, roll, pitch, yaw, angle`。
12. 单项绝对误差阈值统一为 `0.0005`，不因构型不同放宽。

## 2. 当前代码入口

| 模块 | 入口 | 职责 |
| --- | --- | --- |
| `testcase/test_gripper_v2_verify.py` | `TestV2Verify` | 夹爪 V2 全流程和内容验证 |
| `testcase/test_shake_v3_verify.py` | `TestShakeV3Verify` | 摇操 V3 全流程和内容验证 |
| `testcase/test_tianji_v2_verify.py` | `TestTianjiV2Verify` | 天机 V2 全流程和内容验证 |
| `common/fixed_fps_validation.py` | `validate_episode_fixed_fps_timeline` | 独立复算并验证每个 L1 的固定 30 FPS 时间轴 |
| `common/extract_mcap_image.py` | `extract_global_nearest_image_from_mcap_sources` | 在父 L1 内按 `log_time` 提取各相机最近帧 |
| `common/parse_parquet_file.py` | `extract_nearest_parquet_images_batch` | 跨 Parquet 文件命中最近行并提取图片或视频帧 |
| `common/image_compare.py` | `compare_images` | 图片尺寸处理、像素比较和差异图输出 |
| `common/extract_mcap_fields.py` | `extract_robot_vectors_at_time` | 从 MCAP 取最近值、边界值或插值，构造四组七维 |
| `common/extract_parquet_fields.py` | `extract_parquet_robot_vectors_at_time` | 从最近 Parquet 行按 display 映射解析四组七维 |
| `common/extract_parquet_fields.py` | `compare_robot_vectors` | 按 section/group/label 执行逐维误差比较 |
| `common/parse_parquet_file.py` | `compare_parquet_annotations` | 校验标注层级、数量、字段、时间和 L1 播放时长 |

## 3. 构型差异

下表中的差异只决定“从哪里读”和“图片如何保存”，不改变时间对齐及七维取值算法。

| 项目 | 夹爪 V2 | 摇操 V3 | 天机 V2 |
| --- | --- | --- | --- |
| 输出目录 | `lerobot_v2` | `lerobot_v3` | `lerobot_v2` |
| 主相机 | `/sensor/camera_fisheye_r/color/image_raw` | `/gripper/camera_fisheye_r/color/image_raw` | `/camera/bottom_right/image_raw` |
| 相机数量 | 2 | 2 | 4 |
| 位姿 topic | `/pika_pose_l`、`/pika_pose_r` | `/jbt_arm_L/current_arm_tcp_pose`、`/jbt_arm_R/current_arm_tcp_pose` | `/info/eef_left`、`/info/eef_right` |
| 夹爪 topic | `/sensor/gripper_l/data`、`/sensor/gripper_r/data` | `/gripper/gripper_l/data`、`/gripper/gripper_r/data` | `/info/gripper_feedback_L`、`/info/gripper_feedback_R` |
| 夹爪字段 | `angle` | `angle` | `data[0]`，校验时归一化为 `angle` |
| 图片载体 | Parquet 图片 struct | Parquet 图片 struct | S3 视频帧 |
| 标注覆盖 | 1 个 L1、2 个 L2 | 1 个 L1 | 2 个 L1、4 个 L2、3 个 L3 |

位姿字段三类构型一致：

```text
pose.position.x
pose.position.y
pose.position.z
pose.orientation.x
pose.orientation.y
pose.orientation.z
pose.orientation.w
```

相机配置中，夹爪和摇操左相机为 `sync_policy="3"`、右主相机为 `sync_policy="exact_frame"`；天机四路相机均为 `sync_policy="3"`。当前图片验证不依据该字段切换算法，而是在父 L1 内直接选择各相机 `log_time` 最近帧；3 ms 分支判断用于第 10 节的机器人运动数据取值。

## 4. 总体验证流程

三个用例按相同顺序完成内容验证：

1. 读取任务和存储配置，确认任务存在且状态允许验证。
2. 已转换任务直接读取已有产物，不重新创建标注或转换任务。
3. 枚举目标输出目录下的全部 Parquet 文件，至少存在一个文件才继续。
4. 对每个 L1 独立复算固定 30 FPS 时间轴并与 Parquet 全量输出行比较。
5. 对每段 L1/L2/L3 标注的开始和结束时间，命中最近 Parquet 行。
6. 使用该行 `original_timestamp_ns` 回查父 L1 范围内的 MCAP 图片和机器人 topic。
7. 比较原始图片与转换图片。
8. 比较 MCAP 复算七维与 Parquet 七维。
9. 比较 Parquet 标注与期望标注，并校验 L1 播放时长。
10. 任一必要文件、topic、列、图片、字段或比较项失败，当前检查项失败。

## 5. 时间字段和 Episode 范围

### 5.1 唯一时间字段

所有 MCAP 取值逻辑统一使用：

```python
message.log_time
```

包括：

- 父 L1 范围筛选；
- 主相机首帧和末帧；
- 图片最近帧选择；
- state/action 最近消息距离；
- 插值前点和后点；
- 插值比例；
- 有限时间窗口查询。

结果中继续记录 `matched_publish_ns`，但它只用于诊断 `log_time` 与 `publish_time` 的差异，不能参与取值。

### 5.2 闭区间规则

对每个抽检点先确定：

```text
episode_start_ns = parent_l1.startTimeNs
episode_end_ns   = parent_l1.endTimeNs
```

有效消息必须满足：

```text
episode_start_ns <= message.log_time <= episode_end_ns
```

底层读取 API 的结束时间为开区间，因此代码传入 `episode_end_ns + 1`，实现业务上的闭区间。

有 Episode 范围时，即使启用公共函数的 fallback，也只能在完整 Episode 内回退扫描，禁止跨 L1 取消息。

### 5.3 L2/L3 父 L1 映射

夹爪的 L2 均使用步骤 3 创建或配置的唯一 L1 范围。

天机根据标注配置的 `current_sequence` 将 L2/L3 唯一映射到所属 L1。映射不到或映射出多个 L1 时直接报错，不允许按时间猜测父级。

## 6. 固定 30 FPS 时间轴验证

此项对每个 L1 的有效输出行执行全量检查，不是起止点抽检。

### 6.1 时间轴生成规则

在父 L1 闭区间内按主相机 `log_time` 查找：

```text
T0 = 第一条主相机消息的 log_time
last_main_frame = 最后一条主相机消息的 log_time
timeline_end = min(L1.endTimeNs, last_main_frame)
```

第 `n` 条输出的期望值为：

```text
timestamp = n / 30
original_timestamp_ns = T0 + round((n / 30) * 1_000_000_000)
```

期望输出行数为：

```text
floor((timeline_end - T0) * 30 / 1_000_000_000) + 1
```

### 6.2 校验条件

1. 首行 `timestamp` 必须为 0，允许误差 `1e-9 s`。
2. 首行 `original_timestamp_ns` 与 `T0` 的差必须 `<= 1,000 ns`。
3. 每行 `timestamp` 必须落在 `n / 30` 网格，允许 float32 序列化误差 `2e-5 s`。
4. 每行 `original_timestamp_ns` 与固定网格目标差必须 `<= 1,000 ns`。
5. 由 `timestamp` 推导的帧序号必须从 0 开始连续递增。
6. 不允许重复帧、倒序或跳帧。
7. 实际行数必须与公式计算的期望行数一致。

夹爪、摇操、天机的固定 30 FPS 时间轴验证均已执行通过。

## 7. Parquet 抽检行选择

每个标注开始或结束时间为 `annotation_target_ns`。校验会跨全部 Parquet 文件和 row group，在 `original_timestamp_ns` 中选择绝对差最小的一行：

```text
matched_row = argmin(abs(original_timestamp_ns - annotation_target_ns))
mcap_target_ns = matched_row.original_timestamp_ns
```

后续图片、state、action 必须共同使用 `mcap_target_ns`。禁止：

- 直接使用标注边界作为 MCAP 目标；
- 从浮点 `timestamp` 再构造另一个纳秒目标；
- 使用某路图片实际命中时间作为机器人 topic 目标；
- 将机器人 topic 再对齐到最近主相机实际消息。

## 8. 图片取图逻辑

### 8.1 Parquet 侧

夹爪和摇操从命中行的图片 struct 中读取 `bytes`：

1. 优先按标准图片格式解码；
2. 标准图片解码失败时尝试按 ROS2 Image 内容解码；
3. 无法生成可比较 PNG 时失败。

同一时间点的左右图片必须来自同一 Parquet 行。

天机根据命中 Parquet 行的行号及相机列，从对应 S3 视频中提取同序号帧。四路相机列为：

```text
left_eye
right_eye
left_image
right_image
```

缺少视频、相机列、目标帧或图片解码失败时直接失败。

### 8.2 MCAP 侧

对每个相机 topic 独立执行：

```text
matched_message = argmin(abs(message.log_time - mcap_target_ns))
```

候选消息必须位于父 L1 闭区间内。左右或四路相机允许命中不同的实际 `log_time`，但目标时间必须相同。

图片取帧没有“超过 3 ms 就插值或失败”的分支，也不会对图片做时间插值。代码先选择父 L1 内 `log_time` 距离最小的一条消息，再要求该消息能够成功解析为图片；最近消息无法解析时直接失败，不会跳过它改选次近消息。

远端 MCAP 必须存在 chunk index。搜索先从目标附近的小窗口开始并逐级扩大；仍缺消息时可扫描完整父 L1，但不得退回无边界全文件扫描。必要相机 topic 缺失或最近消息无法解析时失败。

### 8.3 图片比较

公共比较先将两张图转换为 RGB，计算每个像素三个颜色通道的绝对差：

```text
per_pixel_diff = max(abs(R1-R2), abs(G1-G2), abs(B1-B2))
matched_pixel = per_pixel_diff <= 10
```

最终必须同时满足匹配像素比例和全图 RGB 平均绝对误差。

| 规则 | 夹爪、摇操 | 天机 |
| --- | --- | --- |
| 尺寸 | 必须一致 | MCAP 参考图 BICUBIC 缩放到视频帧尺寸 |
| 高斯平滑 | 不使用 | 两侧均使用 `radius=1.0` |
| 单像素阈值 | `<= 10` | `<= 10` |
| 匹配像素比例 | `>= 0.95` | `>= 0.90` |
| 全图平均绝对误差 | `<= 3.0` | `<= 4.0` |

匹配像素比例和平均绝对误差必须同时通过。最大单点误差只用于诊断，不单独决定结果。失败时生成放大 4 倍差异的 PNG；天机通过后清理临时图片，失败时保留证据。

## 9. Parquet 七维解析

### 9.1 必需列

每个命中行必须提供：

```text
timestamp
original_timestamp_ns
actions
state
ac_display
st_display
```

对应关系为：

| section | 数值列 | 字段映射列 |
| --- | --- | --- |
| `action` | `actions` | `ac_display` |
| `observation_state` | `state` | `st_display` |

### 9.2 字段映射

校验不依赖固定数组切片，而是按照 display 字段枚举顺序建立：

```text
(left/right, x/y/z/roll/pitch/yaw/angle) -> source index
```

天机字段名中包含夹爪 `data[0]` 时，只要字段语义可识别为 gripper，即归一化为 `angle`。

以下情况直接失败：

- `actions` 或 `state` 不是 list；
- `ac_display` 或 `st_display` 不是 dict；
- display 键数量与数值数组长度不同；
- 字段无法识别 left/right；
- 字段无法识别七维标签；
- 同一侧同一标签重复；
- 任一侧缺少任一七维；
- 值不能转换为 float。

每个抽检点最终必须得到：

```text
action/left
action/right
observation_state/left
observation_state/right
```

每组标签固定为：

```text
x, y, z, roll, pitch, yaw, angle
```

## 10. MCAP 七维取值

### 10.1 topic 和阈值

位姿和夹爪 topic 从当前构型 JSON 加载。每个 topic 同时读取自己的 `sync_policy`：

```text
"3"           -> 3_000_000 ns
"exact_frame" -> 3_000_000 ns
缺省           -> 3_000_000 ns
```

若同一 topic 在 action 和 observation_state 中配置了不同阈值，直接失败。

### 10.2 取值决策

所有候选消息先按父 L1 裁剪。对每个 topic 计算 Episode 内最近消息、目标前点和目标后点，然后按以下顺序决策：

```text
没有消息
    -> missing，必要 topic 直接失败

最近距离 <= topic_tolerance
    -> nearest，直接取最近消息

目标早于 Episode 内第一条消息
    -> clamp_first，取第一条

目标晚于 Episode 内最后一条消息
    -> clamp_last，取最后一条

前后点同时缺失，或前后点时间相同
    -> single_value，取唯一有效值

其余情况
    -> interpolated，使用 Episode 内相邻前后消息插值
```

边界钳制使用的是完成 L1 裁剪后的首值和末值。禁止为构造包络引入 L1 外消息。

Episode 内只有一条消息时，若时间差在阈值内则记录为 `nearest`；若目标在该消息之前或之后，则分别记录为 `clamp_first` 或 `clamp_last`。三种情况下数值都来自这条唯一消息。

### 10.3 插值比例

当策略为 `interpolated`：

```text
r = (target_ns - before.matched_log_ns)
    / (after.matched_log_ns - before.matched_log_ns)
```

位置和夹爪：

```text
value = before + (after - before) * r
```

### 10.4 姿态插值和输出顺序

原始四元数顺序固定为：

```text
qx, qy, qz, qw
```

直接取值和插值结果都先归一化四元数。零模长四元数直接失败。

插值采用最短旋转路径 SLERP：

1. 归一化起止四元数；
2. 点积小于 0 时翻转结束四元数，选择最短路径；
3. 两四元数非常接近时使用归一化线性插值，避免数值不稳定；
4. 其余情况按球面线性插值公式计算；
5. 将结果按内禀大写 `ZYX` 转为 `yaw, pitch, roll`；
6. 最终向量重排为 `roll, pitch, yaw`。

禁止先把前后四元数转换成欧拉角，再分别对 roll/pitch/yaw 做线性插值。

### 10.5 action 时间语义

当前验证逻辑中：

```text
state target  = matched Parquet row original_timestamp_ns
action target = matched Parquet row original_timestamp_ns
image target  = matched Parquet row original_timestamp_ns
```

action 不自动移动到下一帧，也不使用 `next_state` 语义。

## 11. 七维比较

比较键固定为：

```text
(section, group, label)
```

即先匹配 `action/observation_state`，再匹配 `left/right`，最后按七维标签匹配，不依赖两边数组的偶然排列。

每一维计算：

```text
absolute_difference = abs(mcap_value - parquet_value)
passed = absolute_difference <= 0.0005
```

一个时间点包含：

```text
4 组 * 7 维 = 28 项
```

任一项超限，则该组和该时间点失败；任一抽检时间点失败，则对应构型七维验证失败。阈值对位置、姿态和夹爪统一使用绝对误差，没有按维度设置不同阈值。

结果中保留：

- annotation target；
- Parquet matched target；
- MCAP sampling reference；
- `time_field=log_time`；
- 父 L1 起止时间；
- topic tolerance；
- `nearest/interpolated/clamp_first/clamp_last/single_value` 策略；
- 最近消息、前后消息和插值比例；
- Parquet source index 和 display 字段；
- 每维两侧数值、绝对差和通过状态。

## 12. 标注验证

Parquet 标注摘要与当前任务提交的期望标注逐层比较。已转换任务不重新提交标注，使用用例配置中的同一份期望值。

检查内容：

1. L1/L2/L3 层级是否存在且类型一致；
2. 每层 segment 数量是否一致；
3. `layer_id` 是否一致；
4. 提供 segment ID 时是否一致；
5. description/name 是否一致；
6. `startTimeNs/endTimeNs` 是否一致；
7. 是否存在缺失或额外 segment；
8. Parquet 标注结构解析是否存在 validation error；
9. L1 的 Parquet 播放时长与绝对标注时长是否一致。

一条实际 segment 只能匹配一条期望 segment，不能重复匹配掩盖缺失。

L1 播放时长：

```text
expected_duration = (endTimeNs - startTimeNs) / 1e9
parquet_duration = last_timestamp - first_timestamp
duration_error = abs(expected_duration - parquet_duration)
passed = duration_error <= 0.07 s
```

## 13. 当前覆盖和通过结果

| 构型 | 标注段 | 抽检时间点 | 图片 | 七维标量 | 固定 FPS | 标注 | 总结果 |
| --- | ---: | ---: | ---: | ---: | --- | --- | --- |
| 夹爪 V2 | 3 | 6 | 12 / 12 | 168 / 168 | 通过 | 通过 | 通过 |
| 摇操 V3 | 1 | 2 | 4 / 4 | 56 / 56 | 通过 | 通过 | 通过 |
| 天机 V2 | 9 | 18 | 72 / 72 | 504 / 504 | 通过 | 通过 | 通过 |
| 合计 | 13 | 26 | 88 / 88 | 728 / 728 | 全部通过 | 全部通过 | 全部通过 |

图片结果：

- 夹爪 12 组和摇操 4 组均为 100% 匹配，平均绝对误差为 0。
- 天机 72 组全部通过；最低匹配像素比例 `90.531%`，最高平均绝对误差 `3.6488`，均在阈值内。

标注播放时长结果：

- 夹爪误差 `0.0137 s`；
- 摇操误差 `0.0260 s`；
- 天机两个 L1 误差分别为 `0.0453 s`、`0.0280 s`；
- 全部 `<= 0.07 s`。

边界规则修正后的真实数据复算：

- 摇操 L1 起点：位姿使用 `clamp_first`，最大七维差值约 `4.81e-7`，通过；
- 夹爪 L1 终点：位姿和夹爪使用 `clamp_last`，最大七维差值约 `5.77e-7`，通过。

天机此前未暴露边界差异，是因为其机器人数据频率较高，已诊断样本的最近距离全部在 3 ms 内，走 `nearest` 即可通过。这不代表天机使用不同规则。

## 14. 失败判定和证据

以下任一情况应直接失败，不允许通过放宽阈值掩盖：

- 目标 Parquet 文件不存在；
- 必需列、图片、视频、topic 或字段缺失；
- 固定 30 FPS 时间轴首帧、网格、连续性或行数不一致；
- MCAP 使用了 L1 外消息；
- 图片无法解码或图片指标未达标；
- 四组七维不完整、标签重复或 display 映射失败；
- 任一七维差值大于 `0.0005`；
- 标注缺失、多余、层级错误、文案错误或时间错误；
- L1 播放时长误差大于 `0.07 s`。

失败报告应至少保留目标时间、实际命中时间、父 L1 范围、来源文件、topic、取值策略、插值端点和比例、逐维差值、图片指标及标注差异。图片失败时保留原图、转换图和差异图。

## 15. 开发防回归清单

开发对照实现时必须确认：

1. 是否所有时间筛选和插值都使用 `log_time`，没有混入 `publish_time`。
2. 是否先按父 L1 闭区间裁剪所有相机、位姿和夹爪 topic。
3. 是否正确把天机 L2/L3 映射到父 L1，而不是用自身标注范围裁剪。
4. 是否以父 L1 主相机第一条 `log_time` 为 T0。
5. 是否按 `T0 + n/30` 生成连续输出，并正确写入 `timestamp` 和 `original_timestamp_ns`。
6. 是否让同一输出行的图片、state、action 使用同一个 `original_timestamp_ns`。
7. 是否按每个 topic 的 `sync_policy` 判断 `nearest`，且比较条件为 `<=`。
8. 是否在目标早于首值或晚于末值时使用 Episode 内 `clamp_first/clamp_last`。
9. 是否只用 Episode 内前后消息插值，没有跨 L1 补包络。
10. 是否对位置和夹爪线性插值、对姿态四元数执行最短路径 SLERP。
11. 是否使用 `[qx,qy,qz,qw]` 顺序和内禀大写 `ZYX`，最终输出 `roll,pitch,yaw`。
12. 是否确保 action 不做下一帧偏移。
13. 是否通过 display 字段映射左右和七维，不使用固定数组切片猜测。
14. 是否保持图片、七维和标注现有阈值，不因历史错位问题放宽标准。
15. 是否保留足够诊断元数据，使任何失败都能定位到具体 Episode、时间、topic、来源行和数值维度。

## 16. 防回归测试

当前相关单元测试覆盖：

- `log_time` 与 `publish_time` 不同时必须按 `log_time` 选择；
- `sync_policy` 数字值及 `exact_frame` 解析；
- `nearest`、`interpolated`、`clamp_first`、`clamp_last`；
- 主相机和图片 fallback 不得越过父 L1；
- 四元数最短路径 SLERP 及实际七维提取路径；
- 固定 FPS 首末主相机帧使用 `log_time`；
- 天机 L2/L3 的父 L1 映射。

相关定向测试当前为 `24 passed`，静态编译通过。

## 17. 最终开发结论

当前验证代码已经与开发确认的转换逻辑统一：以 `log_time` 和父 L1 为唯一原始时间边界，以 Parquet 行的 `original_timestamp_ns` 为图片/state/action 共同目标，按 topic 阈值执行最近邻、Episode 内插值或首尾钳制，姿态使用四元数 SLERP，action 不移帧。

夹爪、摇操、天机均已用同一核心规则验证通过。后续修改任一转换端或验证端实现时，应以第 1 节统一口径和第 15 节防回归清单为准，同时更新两侧测试，避免再次出现跨 L1、时间字段不一致或姿态算法不一致。
