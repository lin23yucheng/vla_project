V1.0夹爪、摇操、天机数据验证报告

1. 报告目的
本报告说明夹爪、摇操和天机三类机器人数据在完成转换后，如何确认转换结果可用、内容正确且可追溯。
验证以转换前的原始数据为基准，分别核对：
- 图像是否来自正确的相机和正确的动作时刻；
- 机械臂位置、姿态和夹爪开合等运动数据是否保持一致；
- 人工标注的层级、文字和时间范围是否被完整保留；
- 转换任务、输出文件和数据内容是否形成完整闭环。
原始数据保存在 MCAP 文件中，转换结果以 Parquet 数据文件形式保存。两者均从同一任务的数据存储位置读取。

2. 验证对象与抽检范围
测试对象	输出版本	相机数量	标注覆盖	图片对比数量	向量数值对比数量
夹爪构型	V2	左右 2 路	1 段一级标注、2 段二级标注	12 组	168 项
摇操构型	V3	左右 2 路	1 段一级标注	4 组	56 项
天机构型	V2	4 路	2 段一级、4 段二级、3 段三级标注	72 组	504 项
抽样不是随机挑选，而是覆盖每段标注的开始时刻和结束时刻。动作边界通常是数据转换或标注对齐最容易出现问题的位置，因此这种取样方式能更有效发现问题。
例如，天机共有 9 段标注，每段检查起止两个时间点，并对每个时间点检查 4 路相机，共 72 组图像。每个时间点还核对左右臂在 “控制指令” 和 “实际状态” 两类数据中的 4 组七维运动数据。

3. 总体验证流程
验证按如下顺序执行：
1. 确认任务存在、状态正确，并读取该任务的原始数据和转换结果。
2. 确认转换已经完成，并在该任务的转换结果目录中发现至少一个 parquet 数据文件。
3. 以各段标注的起止时间为依据，分别从原始数据和转换结果中寻找对应的相机图像。
4. 对原始图像与转换后图像进行像素级一致性比较。
5. 先命中转换结果的固定 FPS 输出帧，再在相同时间点提取机械臂和夹爪运动数据，逐项比较数值误差。
6. 读取转换结果中的标注信息，与提交的标注内容逐项核对。
7. 保存每一步的时间、文件、对比指标和失败证据，便于复核。
若任务已经完成过转换，验证会直接读取既有结果进行内容核验，不会重复创建任务或修改已存在的数据。

4. 如何寻找和对齐图像
4.1 转换结果中的图像
对于每一个标注起点或终点，系统在全部转换结果中寻找时间最接近的一条数据记录，并导出其中对应相机的图像。每次取图都会记录：
- 标注要求检查的时间；
- 实际命中的数据时间；
- 两个时间相差多少；
- 图像来自哪个输出文件；
- 图像是否成功解码和落盘。
夹爪和摇操数据直接读取数据文件中保存的相机图像。天机数据的相机图像以视频帧形式保存，因此会根据记录中的视频位置提取相应帧，再转换成可比较的 PNG 图像。
4.2 原始数据中的图像
原始数据按相机分别记录。系统依据机器人构型确认需要检查的相机：夹爪、摇操必须存在左、右相机；天机必须存在 4 路相机。
对每一条相机数据，系统在目标时间附近查找发布时间最接近的一帧。检索优先使用数据文件中已有的时间索引，只读取目标时段附近的数据，不会无差别读取全部原始数据。查找结果会记录实际取到的时间、与目标时间的差、来源文件和扫描情况。
如果没有找到相机数据、相机消息无法解码，或者图像文件没有成功生成，该检查项直接判定失败。
4.3 原始与转换图像的时间对齐原则
测试对象	对齐原则	目的
夹爪	先确定转换结果实际命中的固定 FPS 帧，再在原始数据中按该时刻寻找最近帧	排除两边采样频率不同造成的“看起来不同帧”问题。
摇操	先确定转换结果实际命中的固定 FPS 帧，再在原始数据中按该时刻寻找最近帧	排除两边采样频率不同造成的“看起来不同帧”问题。
天机	先确定转换结果实际命中的固定 FPS 帧，再在原始数据中按该时刻寻找最近帧	适配视频帧和原始相机帧的时间对齐需求。
三种构型均先以标注边界时间命中最近的 Parquet 输出行，再以该行的实际时间作为原始图像取帧目标。两侧均会保留标注时间、Parquet 命中时间和原始图像实际命中时间，便于排查时间错位。

5. 图像如何比较，允许误差多少
图像对比采用像素级比较。两张图会统一为 RGB 彩色图，然后逐个像素计算红、绿、蓝三个通道的差异。
一个像素是否可视为一致，取决于其三个颜色通道中差异最大的一个；只要最大差异不超过设定容差，该像素即计为匹配。最终同时计算 “容差内像素占比” 和 “平均绝对误差”。两个条件必须都满足，整张图才通过。
指标	夹爪、摇操	天机
图像尺寸	必须完全一致	允许原始图与转换图尺寸不同
尺寸处理	不做缩放	原始图缩放到转换图尺寸后比较
平滑处理	不做平滑	两边均进行轻度平滑处理
单个像素允许的最大颜色差	不超过 10 个色阶	不超过 10 个色阶
满足像素容差的像素占比	不低于 95%	不低于 90%
全图平均绝对误差	不高于 3.0	不高于 4.0
天机用例允许原始图与转换后视频帧尺寸不同：比较时会将原始图缩放到转换图尺寸。缩放和视频编码会带来边缘及纹理上的轻微差异，因此天机允许缩放、轻度平滑和相对宽松的图像阈值。夹爪和摇操的图像尺寸必须直接一致，因此维持更严格的原图比较要求。
“最大单点差异” 会被记录，供排查使用，但不单独决定通过或失败。图像必须同时满足像素匹配率和平均误差两项要求。
当两张图尺寸可比较但像素指标不通过时，系统会生成一张差异图，并将细小色差放大 4 倍显示。尺寸不一致且未启用缩放的夹爪、摇操用例会直接失败，不生成像素差异图。差异图可直接用于判断问题是整图错帧、局部遮挡、颜色偏移或视频压缩痕迹。

6. 如何寻找机器人运动数据
每个检查时间点都需要得到四组运动数据：
数据类别	左侧	右侧
控制指令	左臂控制向量	右臂控制向量
实际状态	左臂状态向量	右臂状态向量
每组均包含 7 个数值：三维位置、三个姿态角和夹爪开合角度。
6.1 在转换结果中寻找向量
固定 FPS 输出的时间轴生成规则为：在 Episode 标注范围内找到第一张主相机帧 T0，然后生成 T0 + n / 30 的均匀时间轴。系统先以标注边界时间寻找最近的一行转换结果，再从其中读取控制指令和实际状态；该行的 `original_timestamp_ns` 即后续原始数据采样的唯一时间基准。它会依据数据中的字段说明确认每个数值属于左臂或右臂，并确认七个维度齐全。
以下情况会直接失败：控制或状态数据不是有效数值数组、数值数量与字段说明不一致、左右臂无法识别、七个维度有缺失、字段重复，或标签与数值无法对应。
每次结果都会保留实际命中时间、时间差、来源文件和记录位置，确保每一个数值都能追溯到具体转换结果。
6.2 在原始数据中寻找向量
原始数据中的机械臂状态以 Parquet 实际命中的固定 FPS 时间点同步，不再二次对齐到主相机原始帧。具体原则如下：
1. 以 Parquet 实际命中行的 `original_timestamp_ns` 作为 state/action 的采样基准。
2. 在该基准时间附近查找左右臂位姿和夹爪开合数据。
3. 如果最近一条数据距基准时间不超过 3 毫秒，直接使用该值。
4. 如果时间差超过 3 毫秒，但基准时间前后都有有效数据，则按时间位置进行插值，得到基准时刻的估算值。
5. 位置信息和夹爪角度使用线性插值；姿态使用适合旋转数据的平滑插值方式，避免姿态角跳变。
6. 如果时间差超过 3 毫秒且无法形成前后包络，仍会保留最近值，并在结果中以 `nearest_missing_bracket` 策略记录该情况，供人工判断。
原始数据结果会保存使用的策略、相邻数据时间、插值比例、固定时间轴采样时间和来源信息。因此即使数值失败，也能判断是转换值错误、源数据不同步还是时间插值造成的偏差。夹爪、摇操和天机均遵循这一规则。

7. 向量如何比较，允许误差多少
四组数据按 “控制 / 状态” 和 “左 / 右” 一一对应比较。每组七个维度也按明确的名称对应，不会仅依赖数组顺序。
比较前必须满足：
- 原始数据和转换结果都存在四组向量；
- 每组都有且只有七个预期维度；
- 两边维度名称完全一致；
- 每一个维度都能取得有效数值。
每个数值的绝对差按下式计算： 绝对误差 = 原始数值与转换后数值之差的绝对值
通过条件为：每一个位置、姿态和夹爪角度数值的绝对误差均不超过 0.0005。等于该值时仍视为通过。
任一时间点、任一左右臂、任一控制或状态组、任一维度超过此阈值，整项向量校验即失败。失败记录会显示原始值、转换值和绝对误差；原始数据与转换结果的实际取样时间、固定时间轴采样时间及插值信息保存在对应的提取结果附件中。
该阈值对位置、姿态和夹爪角度统一采用绝对误差判断。因此出现失败时，应先确认双方是否取到了同一时刻的数据、原始数据是否使用了插值，再判断是否存在真实转换偏差。

8. 标注信息如何比较
转换结果中的标注信息会与任务中提交的标注逐项核对。对于已完成转换、无需再次提交标注的任务，则使用该测试对象预先定义的同一份标注要求作为对照基准。
检查内容包括：
检查项	通过要求
标注层级	一级、二级、三级标注数量与要求一致。
标注数量	每个层级中的标注段数完全一致。
标注归属	每段属于正确层级。
标注编号	当任务提供标注编号时，必须一致。
标注文案	动作描述必须一致。
开始与结束时间	时间边界必须完全一致。
多余或缺失标注	不允许缺少要求的标注，也不允许出现未提交的额外标注。
一级标注播放时长	转换结果中的播放时长与标注时长误差不超过 0.07 秒。
标注比较会优先在同一层级中寻找相同编号的段；若没有可用编号，再结合动作描述和起止时间寻找对应段。每一条实际标注只能匹配一次，避免一条数据被重复用来掩盖缺失项。
因此，标注验证不仅检查有没有文字，还验证动作语义、时间边界、层级关系和播放时间轴是否完整地随数据转换保留下来。

9. 失败后的证据与排查顺序
每次验证都会保留可复核的结果信息，包括实际抽样时间、来源文件、对比指标、数值明细、标注对比结果和失败原因。夹爪、摇操用例会附加原始图与转换图；天机用例仅在图片校验失败时保留并附加两张原图及差异图，校验通过的临时 PNG 会被清理。建议按以下顺序排查：
1. 确认任务和转换状态正确，转换结果目录中已发现 parquet 数据文件。
2. 比较原始数据与转换结果实际命中的时间，确认没有因采样频率不同而选中不同帧。
3. 图像失败时，查看原始图、转换图、尺寸、像素匹配率、平均误差和差异图。
4. 向量失败时，定位具体的时间点、左右臂、控制或状态类别和维度，再查看原始数据使用的是最近值还是插值。
5. 标注失败时，区分是层级数量、标注编号、动作描述、起止时间、额外标注还是播放时长问题。

10. 验收结论的含义
三类检查共同通过，表示：
- 原始相机画面已被正确转换和保存；
- 机器人运动状态和控制信息在严格数值误差内保持一致；
- 标注文本、层级和时间轴没有遗漏、错位或篡改；
- 每一个检查结论均可回溯到对应任务、数据文件、实际时间点和对比证据。
也就是说，该验证覆盖了数据从原始采集、标注、转换到最终训练数据文件的完整正确性链路。

11. 真实数据验证结果
基于夹爪、摇操、天机三类构型的实际数据，严格执行前述验证流程，输出完整校验结果与明细。所有验证均以原始 MCAP 采集数据为基准，对转换输出的 Parquet 文件执行全项核对。

本报告规定所有构型均按固定 30 FPS 输出时间轴验证：在 Episode 标注范围内定位首张主相机帧 T0，转换输出帧时间为 T0 + n / 30。每个标注边界先命中最近的 Parquet 输出帧；MCAP 图像按该时刻取最近帧，state/action 按该时刻取最近值或插值。向量比较不再二次对齐到主相机原始帧。
11.1 验证结果总览
测试构型	输出版本	"图像校验 （通过 / 总数）"	"七维向量校验 （通过 / 总数）"	标注信息校验	固定 FPS 验证状态
夹爪构型	V2	12 / 12	168 / 168	通过	已通过
摇操构型	V3	4 / 4	56 / 56	通过	已通过
天机构型	V2	72 / 72	504 / 504	通过	已通过
三类构型的图像一致性、运动数据精度和标注完整性均已在固定 FPS 规则下满足预设验收阈值，数据转换结果正确、链路完整且可追溯。
11.2 夹爪构型（V2 版本）
本次验证覆盖 3 段标注（1 段一级、2 段二级）的起止时间边界，共 6 个校验时间点，同步核对左右 2 路相机图像与对应机械臂运动数据。
11.2.1 图像校验
- 校验范围：3 段标注 × 2 个时间点 × 2 路相机 = 12 组图像
- 图像尺寸：统一为 640×480，无缩放处理
- 校验结果：12 组图像全部通过；所有组别的容差内像素占比均为 100.0000%，平均绝对误差均为 0.0000，原始数据与转换后图像像素完全一致。
- 分项耗时：步骤 3 标注对比耗时 0.120s，步骤 4 标注对比耗时 0.121s，步骤 5 标注对比耗时 0.110s。
11.2.2 运动向量校验
- 校验范围：3 段标注 × 2 个时间点 × 4 组向量 × 7 个维度 = 168 项数值
- 校验阈值：单项绝对误差 ≤ 0.0005
- 校验结果：168 项数值全部通过；所有时间点的左右臂控制指令、实际状态七维数据，与原始数据差值均在阈值范围内。
- 分项耗时：步骤 3 向量对比耗时 0.003s，步骤 4 向量对比耗时 0.004s，步骤 5 向量对比耗时 0.004s。
11.2.3 标注信息校验
- 校验内容：标注层级、段数、时间边界、一级标注播放时长
- 播放时长校验：Parquet 计算时长 5.8333 秒，标注基准时长 5.847 秒，实际误差 0.0137 秒，小于允许误差 0.07 秒，校验通过。
- 整体结论：标注层级、数量、文案与时间边界完全一致，无多余或缺失标注，校验通过。
- 耗时：0.363s。
11.3 摇操构型（V3 版本）
本次验证覆盖 1 段一级标注的起止时间边界，共 2 个校验时间点，同步核对左右 2 路相机图像与对应机械臂运动数据。
11.3.1 图像校验
- 校验范围：1 段标注 × 2 个时间点 × 2 路相机 = 4 组图像
- 图像尺寸：统一为 640×480，无缩放处理
- 校验结果：4 组图像全部通过；所有组别的容差内像素占比均为 100.0000%，平均绝对误差均为 0.0000，原始数据与转换后图像像素完全一致。
- 耗时：0.120s。
11.3.2 运动向量校验
- 校验范围：2 个时间点 × 4 组向量 × 7 个维度 = 56 项数值
- 校验阈值：单项绝对误差 ≤ 0.0005
- 校验结果：56 项数值全部通过；所有时间点的左右臂控制指令、实际状态七维数据差值均在阈值范围内。
- 耗时：向量提取耗时 27.177s，对比校验耗时 0.002s。
11.3.3 标注信息校验
- 校验内容：标注层级、段数、时间边界、一级标注播放时长
- 播放时长校验：Parquet 计算时长 9.8000 秒，标注基准时长 9.826 秒，实际误差 0.0260 秒，小于允许误差 0.07 秒，校验通过。
- 整体结论：标注内容完全一致，失败项为 0，校验通过。
- 耗时：0.486s。
11.4 天机构型（V2 版本）
本次验证覆盖 9 段标注（2 段一级、4 段二级、3 段三级）的起止时间边界，共 18 个校验时间点，同步核对 4 路相机图像与对应机械臂运动数据。
11.4.1 图像校验
- 校验范围：9 段标注 × 2 个时间点 × 4 路相机 = 72 组图像
- 预处理规则：原始 MCAP 图像从 960×744 通过 BICUBIC 算法缩放到 640×480；原始图与转换图均执行半径 1.0 的高斯平滑处理后再执行像素对比。
- 校验结果：72 组图像全部通过。 
  - 像素匹配率：最低 90.531%（出现在 l1_02、l2_04 标注结束时间的左相机），满足≥90% 的验收要求；
  - 平均像素误差：最高 3.6488（与最低匹配率为同组），满足≤4.0 的验收要求；
  - 最大单点像素误差：最高 67 色阶，仅作问题排查参考，不单独决定通过判定。
- 耗时：158.717s。
11.4.2 运动向量校验
- 校验范围：18 个时间点 × 4 组向量 × 7 个维度 = 504 项数值
- 校验阈值：单项绝对误差 ≤ 0.0005
- 对齐方式：以 Parquet 实际命中帧的固定时间轴 T0 + n / 30 为基准，在该时刻对 MCAP 原始运动数据取最近值或插值；双方采样时间完全一致，不再二次对齐到主相机原始帧。
- 校验结果：504 项数值全部通过；所有时间点的左右臂控制指令、实际状态七维数据差值均在阈值范围内。
- 耗时：700.512s。
11.4.3 标注信息校验
- 校验内容：三级标注层级、段数、编号、文案、时间边界、一级标注播放时长
- 一致性结论：标注层级、归属、数量、文案与时间边界完全一致，无多余或缺失标注，失败项为 0。
- 播放时长校验： 
  - L1 第 1 条：Parquet 时长 47.2667 秒，标注基准 47.312 秒，误差 0.0453 秒；
  - L1 第 2 条：Parquet 时长 27.9000 秒，标注基准 27.928 秒，误差 0.0280 秒；
  - 两项误差均小于 0.07 秒允许值，校验通过。
- 耗时：59.987s。
11.5 最终验证结论
夹爪 V2、摇操 V3、天机 V2 均已按固定 FPS 规则完成验证，在图像一致性、运动数据精度和标注完整性三个维度均满足验收标准：
1. 抽检时间点的原始相机画面已被正确转换与存储，未发现错帧、错位或画质异常丢失；
2. 机械臂位姿与夹爪开合数据的全部抽检项误差均在允许阈值内；
3. 全层级标注信息完整保留，时间轴与动作语义未发现错位或遗漏。
整体验证链路完整可追溯，转换结果符合数据使用要求，可用于后续模型训练与分析。
